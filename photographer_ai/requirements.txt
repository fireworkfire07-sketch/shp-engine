# Photographer AI - core dependencies
# All of these are real, working libraries (no placeholder/fake deps).
numpy>=1.26
Pillow>=10.0
opencv-python-headless<5   # pinned <5: v5 dropped bundled Haar cascade data files we rely on
imagehash>=4.3             # perceptual hashing for duplicate / near-duplicate / burst detection
rawpy>=0.27                # RAW decoding (CR2, CR3, NEF, ARW, RAF, DNG) via bundled libraw
pillow-heif>=0.14          # HEIC/HEIF decoding
